services:
  main-app:
    build:
      context: ../
      dockerfile: Dockerfile

    environment:
      DB_URI: ${DB_URI:?}
      NATS_URI: ${NATS_URI:?}
      REDIS_URI: ${REDIS_URI:?}

    container_name: main-app
